apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ing-bookmedik
spec:
  rules:
  - host: www.bookmedik.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: bookmedik
            port:
              number: 80
